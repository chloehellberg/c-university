@{
  Layout = "_Layout";
}

@model University.Models.Student
@Html.ActionLink(" All Students", "Index", null, new { @class="btn btn-info fa fa-graduation-cap" })
<h2>Are you sure you want to delete this?</h2>

@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)
@using (Html.BeginForm())
{
  <button type="submit" class="btn btn-info fa"><i class="fa-minus-circle"> Delete</i></button>
}